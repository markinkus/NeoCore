# Issue Starter Backlog

Use these as ready-to-publish GitHub issues.

1. Add AUD currency to `CURRENCY_REGISTRY` + tests (`good first issue`, `help wanted`).
2. Add CAD currency to `CURRENCY_REGISTRY` + tests (`good first issue`, `help wanted`).
3. Improve `InsufficientFundsError` message with policy mode context (`good first issue`).
4. Add tests for `OverdraftPolicy.overdraft_limit` edge-case at exact threshold (`good first issue`).
5. Add docs example for multi-currency transaction rejection (`docs`).
6. Add `LedgerEngine.get_statement(..., as_of)` usage example to README (`docs`).
7. Add benchmark script comparing MemoryStore vs SQLiteStore posting throughput (`design`, `help wanted`).
8. Prototype `PostgresStore` protocol mapping (`design`).
9. Add reversible capture template with explicit original tx linkage (`design`).
10. Extend payment rail scenario with partial settle and residual fee handling (`help wanted`).
11. Add JSON schema for template definitions (`design`).
12. Add troubleshooting section for idempotency collisions and retries (`docs`, `good first issue`).
